{
  "name": "moonwick",
  "private": true,
  "version": "0.1.0",
  "description": "Moonwick — jeu mobile near-miss : une sorcière sur balai charge sa magie en frôlant les obstacles.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
